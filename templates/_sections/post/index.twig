{% extends "_layouts/main.twig" %}

{% set entryNavCondition = {section: 'post', orderBy: 'postDate desc'} %}

{% block content %}

    {% include '_sections/post/meta.twig' with {
    	entry
    } only %}

    {{ parent() }}

    {% include '_partials/entries-buttons.twig' with {
        entries: craft.entries.section('topic').relatedTo(entry).all,
        prefix: 'Topic'|t
    } only %}


{% endblock %}