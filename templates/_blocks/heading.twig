{# A heading block. This will always be called within an element styled with Tailwinds prose class,
so no styling needed here #}
{{ tag(block.headingLevel.value, {
    id: block.text ? block.text|id : '',
    text: block.text
}) }}
