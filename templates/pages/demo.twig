{% extends "_layouts/main.twig" %}


{% switch currentSite.handle %}

{% case "de" %}
    {% set site = craft.app.sites.siteByHandle('en') %}

    {% set title = 'Seite ohne Entry' %}
    {% set content = 'Beispiel für eine Seite ohne Entry' %}


{% default %}
    {% set site = craft.app.sites.siteByHandle('de') %}

    {% set title = 'Page without Entry' %}
    {% set content = 'Example for a page without Entry' %}

{% endswitch %}

{% set featuredImage = craft.assets.kind('image').width('>900').orderBy('rand()').one %}

{% block content %}
    {{ content }}
{% endblock %}
