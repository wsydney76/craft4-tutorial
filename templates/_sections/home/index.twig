{% extends "_layouts/main.twig" %}


{% block content %}

    {{ parent() }}

    {% cache %}

        {% include '_partials/entries-list.twig' with {
            heading: 'New Posts'|t,
            query: craft.entries
                .section('post')
                .limit(5)
                .orderBy('postDate desc')
        } only %}


    {% endcache %}

{% endblock %}
